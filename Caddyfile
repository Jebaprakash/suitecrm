{
    frankenphp
}

:8080 {
    root * /app/public
    encode zstd gzip
    
    # Standard FrankenPHP handler
    php_server

    # Fallback to index.php for SuiteCRM/Symfony routing
    try_files {path} {path}/ /index.php?{query}

    file_server
}
